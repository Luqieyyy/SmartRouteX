name: smartroutex_rider
description: "SmartRouteX Rider — Enterprise delivery operations app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^2.6.1
  riverpod_annotation: ^2.6.1

  # Networking
  dio: ^5.7.0

  # Local storage
  hive_flutter: ^1.1.0
  hive: ^2.2.3

  # Navigation
  go_router: ^14.8.1

  # Maps & Location
  google_maps_flutter: ^2.14.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0
  permission_handler: ^11.4.0

  # Barcode scanning
  mobile_scanner: ^6.0.4

  # Image capture
  image_picker: ^1.1.2

  # UI
  intl: ^0.19.0
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0

  # Utilities
  path_provider: ^2.1.5
  connectivity_plus: ^6.1.3
  flutter_secure_storage: ^9.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
